LOGIN ATTEMPT는 많이 보이는데 LOGIN SUCCESS나 LOGIN FAILED가 없습니다. 로그인 요청이 완료되지 않고 중단되는 것 같습니다. 더 확인하겠습니다.


3 actions
핵심 패턴을 발견했습니다. 최근 배포 인스턴스가 여러 번 재시작되면서 DB 연결 오류(Connection terminated unexpectedly)가 반복되고 있습니다. 그리고 Users cache warmed up이 매우 오래 걸립니다 (일부 인스턴스에서 33분 소요).

최근 LOGIN ATTEMPT 이후 VERIFY PASSWORD가 나타나지 않는 이유를 확인합니다.

Checked logs
문제가 확실히 보입니다. 최근 로그에서 LOGIN ATTEMPT 이후 VERIFY PASSWORD가 전혀 나오지 않습니다. storage.verifyPassword()가 호출되면 먼저 getUserByUsername()을 실행하는데, 이 함수가 DB 쿼리 중 행(hang) 상태에 빠지고 있습니다.

getUserByUsername 구현을 확인하겠습니다.


Executed rg -C 15 -n getUserByUsername server/storage.ts
이제 DatabaseStorage의 getUserByUsername을 확인하겠습니다.


5 actions
이제 핵심 원인을 찾았습니다. getCachedUsers() 함수를 확인하겠습니다.


2 actions
원인을 찾았습니다. 정리해드리겠습니다.

로그인 불가 원인 분석
직접적 원인
LOGIN ATTEMPT 로그 후 VERIFY PASSWORD 로그가 전혀 나타나지 않음 → storage.verifyPassword() 내부의 getUserByUsername() 호출이 행(hang) 상태

근본 원인: getCachedUsers() DB 쿼리 행
getUserByUsername()은 먼저 getCachedUsers()를 호출합니다:

async getUserByUsername(username) {
  const cached = await getCachedUsers();  // ← 여기서 멈춤
  ...
}

getCachedUsers() 로직:

async function getCachedUsers() {
  if (usersCache && TTL 안 지남) return usersCache;  // 캐시 히트
  if (usersCacheFetching) return usersCacheFetching;   // 다른 요청이 이미 fetch 중
  
  // DB에서 전체 유저 조회
  usersCacheFetching = db.select().from(users).where(status = "active");
  return usersCacheFetching;
}

문제 시나리오:

배포 환경에서 DB 연결이 불안정 (로그에 Connection terminated unexpectedly 여러 번 발생)
캐시 TTL(5분)이 만료됨
getCachedUsers()가 DB 쿼리를 시작하지만, DB 연결이 죽어있어 응답이 오지 않음
usersCacheFetching Promise가 영원히 pending 상태
이후 모든 로그인 시도가 같은 pending Promise를 await → 전부 행
한편, 이전에 저장된 세션의 get()도 캐시 히트하지 못하면 같은 DB를 쿼리 → 세션 읽기도 실패
배포 인스턴스 불안정 증거
로그에 9번 이상 서버 재시작 확인
Users 캐시 warmup이 33분 걸린 인스턴스 존재
Connection terminated unexpectedly 에러 반복
Cases cache warm-up failed: Connection terminated unexpectedly 발생
요약
구간	상태
DB 연결	불안정 (Neon 서버리스 cold start / 연결 끊김)
Users 캐시	TTL 만료 후 재조회 시 DB hang → 모든 요청 블로킹
로그인	verifyPassword → getUserByUsername → getCachedUsers → DB hang
세션	새 세션마다 다른 ID 발급 (매번 새 연결 시도)
수정 방향
getCachedUsers()에 타임아웃 추가 (예: 10초 초과 시 reject, usersCacheFetching = null 초기화)
DB 쿼리 실패 시 이전 캐시 데이터 반환 (stale-while-revalidate 패턴)
verifyPassword에서 캐시 실패 시 직접 DB 쿼리 (전체 유저 조회 대신 단일 유저 쿼리)
세 가지 핵심 수정:

getCachedUsers()에 타임아웃 + stale 캐시 반환
getCachedCasesRaw()에도 동일한 보호
verifyPassword에서 캐시 실패 시 직접 단일 쿼리 fallback

Edited server/storage.ts
이제 verifyPassword에 캐시 실패 시 직접 단일 쿼리 fallback을 추가합니다.


3 actions
서버가 정상 실행 중입니다. 수정 내용을 정리하겠습니다.

적용된 수정 사항:

DB 쿼리 타임아웃 (15초): getCachedUsers()와 getCachedCasesRaw()에서 DB 쿼리가 15초 이상 걸리면 자동으로 타임아웃 → usersCacheFetching = null 초기화되어 다음 요청이 hang에 빠지지 않음

Stale 캐시 반환: DB 쿼리가 실패해도 이전 캐시 데이터가 있으면 그것을 반환 (30초간 유효) → 서비스 중단 없이 계속 동작

verifyPassword 직접 쿼리 fallback: getUserByUsername()이 캐시 문제로 실패하면, 전체 유저 캐시를 거치지 않고 해당 유저만 직접 DB 쿼리 (10초 타임아웃) → 로그인은 최후까지 시도

이제 명확하게 보입니다! 두 가지 문제:

getCachedUsers() 타임아웃 에러가 unhandled promise rejection으로 프로세스를 크래시 → 서버 재시작 → 반복
verifyPassword fallback 직접 쿼리도 같은 이유로 실패 → users DB 자체가 타임아웃
핵심 문제: warmUpUsersCache()와 로그인의 getCachedUsers()가 타임아웃으로 throw → 캐시가 없으므로 stale 반환도 불가 → unhandled promise rejection → Node.js 프로세스 종료

이제 명확하게 보입니다! 두 가지 문제:

getCachedUsers() 타임아웃 에러가 unhandled promise rejection으로 프로세스를 크래시 → 서버 재시작 → 반복
verifyPassword fallback 직접 쿼리도 같은 이유로 실패 → users DB 자체가 타임아웃
핵심 문제: warmUpUsersCache()와 로그인의 getCachedUsers()가 타임아웃으로 throw → 캐시가 없으므로 stale 반환도 불가 → unhandled promise rejection → Node.js 프로세스 종료

수정 내용 요약:

문제	수정
DB 타임아웃 시 unhandled rejection으로 서버 프로세스 종료	process.on('unhandledRejection') 전역 핸들러 추가로 크래시 방지
DB 쿼리가 무한 대기	pool과 sessionPool에 connectionTimeoutMillis: 10초 설정
캐시 없을 때 throw → 서버 죽음	빈 배열/빈 객체 반환으로 변경 (로그인 시 fallback 직접 쿼리 시도)
Idle 커넥션이 끊겨도 풀에 남음	idleTimeoutMillis: 30초로 불량 커넥션 자동 정리